{% extends "base.html.twig" %}

{% block body %}

<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Edit</th>
                <th>Remove</th>
            </tr>
        </thead>
        <tbody>
            {% for member in members %}
                <tr>
                    <td scope="row">{{member.username}}</td>
                    <td>{{member.mail}}</td>
                    <td>{{member.level}}</td>
                    <td><button type="button" class="btn btn-primary" data-toggle="modal" data-target="#editUserModal">Edit</button></td>
                    <td><button type="button" class="btn btn-primary">Remove</button></td>
                </tr>

                <div class="modal fade" id="editUserModal" tabindex="-1" role="dialog" aria-labelledby="ModalCenterTitle" aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered" role="document">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="ModalLongTitle">Edit User</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <form method="post">
                                <input type="hidden" name="_id" value="{{member.id}}">
                                <div class="form-group">
                                    <input type="text" class="form-control" name="_username" value="{{member.username}}" placeholder="Username">
                                </div>
                                <div class="form-group">
                                    <input type="text" class="form-control" name="_mail" value="{{member.mail}}" placeholder="Email">
                                </div>
                                <div class="form-group">
                                        <label for="FormControlSelect">Roles</label>
                                        <select multiple class="form-control" id="FormControlSelect">
                                          <option>Admin</option>
                                          <option>Support</option>
                                          <option>Police</option>
                                        </select>
                                      </div>
                            </form>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-dark" data-dismiss="modal">Reset Password</button>
                            <button type="button" class="btn btn-primary">Save changes</button>
                        </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </tbody>
    </table>
</div>


{% endblock %}