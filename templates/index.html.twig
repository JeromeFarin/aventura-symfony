{% extends "base.html.twig" %}

{% block body %}

<div class="container">
    {% for category in categories %}
        <div class="category">
            <h3>{{ category.name }}</h3>
            <pre>
                {{ dump(category) }}
            </pre>
        </div>
    {% endfor %}
</div>

{% endblock %}